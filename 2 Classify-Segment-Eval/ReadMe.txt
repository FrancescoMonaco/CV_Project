Note:
The CMAKE has been tested on two different architectures,
in one of them it needed the now commented "set(OpenCV_DIR <path/to/build>)"
in order to be able to make the files

Usage:
<Windows version>
& "path/to/Project_CV.exe" <path/to/Project_Monaco_Russo>
